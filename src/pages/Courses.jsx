import { useState } from 'react'
import { Search, SlidersHorizontal } from 'lucide-react'
import { courses } from '../data/siteData'
import { CourseCard } from '../components/Common'
import { PageIntro } from '../components/Layout'
export default function Courses() { const [search, setSearch] = useState(''); const [category, setCategory] = useState('All'); const [sort, setSort] = useState('Featured'); const categories = ['All', 'JEE', 'NEET', 'CBSE', 'Foundation', 'Class 9–10', 'Class 11–12']; const filtered = courses.filter(c => (category === 'All' || c.category === category) && `${c.name} ${c.description}`.toLowerCase().includes(search.toLowerCase())).sort((a,b) => sort === 'A-Z' ? a.name.localeCompare(b.name) : 0); return <><PageIntro eyebrow="Our programs" title="A course for every ambitious mind." text="From first principles to final revision, find the program that fits your goals and your next big move." /><section className="section courses-page"><div className="container"><div className="filters"><div className="search-box"><Search size={18} /><input value={search} onChange={e => setSearch(e.target.value)} placeholder="Search courses" aria-label="Search courses" /></div><div className="filter-scroll">{categories.map(item => <button className={category === item ? 'filter-active' : ''} onClick={() => setCategory(item)} key={item}>{item}</button>)}</div><label className="sort-select"><SlidersHorizontal size={16} /><select value={sort} onChange={e => setSort(e.target.value)} aria-label="Sort courses"><option>Featured</option><option>A-Z</option></select></label></div><p className="result-count">Showing {filtered.length} programs</p><div className="course-grid">{filtered.map(course => <CourseCard course={course} key={course.id} />)}</div>{filtered.length === 0 && <div className="empty-state">No courses match that search. Try another term.</div>}</div></section></> }
